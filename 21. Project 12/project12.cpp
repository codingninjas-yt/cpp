/*
برنامه ای بنویسید که منوی زیر را داشته باشد
1- login admin
2- login user
3- exit
نام کاربری و رمز عبور پیش فرق برای مدیر دلخواه میباشد
مدیر پس از ورود به محیط کاربری خود میتواند کاربر جدیدی را اضافه کند
و آن کاربر پس از ذخیره شدن میتواند وارد حساب خودش بشود و اطلاعات خود را
ببیند ، در ضمن اطلاعات کابر هم دلخواه است ولی باید حتما نام کاربری و رمز
داشته باشد
*/
#include <iostream>
#include <string>
using namespace std;
int main()
{
    return 0;
}